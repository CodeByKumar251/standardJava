package javaBasics.com.learning.classes.singletonClass;

public class Definition {
	
	/*
	 * The objective of this class is to create only single object
	 * -for example:-If we want to create a DB connection , we want it to be singleton.
	 * 
	 * -Different ways of creating singleton class
	 * a)Eager initialization
	 * b)Lazy initialization
	 * c)Synchronization block
	 * d)Double check Lock(there is a memory issue, resolved through volatile instance variable)
	 * e)Bill Pugh solution
	 * f)Enum singleton
	 * 
	 */

}
