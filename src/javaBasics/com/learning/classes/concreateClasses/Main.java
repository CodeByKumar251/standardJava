package javaBasics.com.learning.classes.concreateClasses;

public class Main {
	
	/*
	 * concreate class:-
	 * These are those class that we can create an instance using NEW keyword
	 * All the method in this class have implementation
	 * It can also be your child class from interface or extend abstract class
	 * a class access modifier can be public or package private(no explicit modifier define)
	 */
	
	public static void main(String args[]) {
		
		Person obj=new Person(2);
		Rectangle obj2=new Rectangle();
		Shape obj3=new Rectangle();
	}

}
