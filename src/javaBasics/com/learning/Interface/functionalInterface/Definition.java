package javaBasics.com.learning.Interface.functionalInterface;

public interface Definition {
	
	/*
	 * if an interface contains only one abstract method, that is known as functional interface.
	 * -@FunctionalInterface keyword can be used at the top of interface (But its optional)
	 * -In functional interface , only one abstract method is allowed but we can have other 
	 * methods like default, static method or methods inherited from the object class
	 */

}
