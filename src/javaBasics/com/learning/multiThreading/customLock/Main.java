package javaBasics.com.learning.multiThreading.customLock;

public class Main {
	
	/*
	 * we know the synchronize works only on share resources(same object)
	 * but when the requirement like if the object will be different then also there should be
	 * lock then we used custom lock. There are 4 types of custom lock
	 * 1)ReentrantLock
	 * 2)ReadWriteLock
	 * 3)StampedLock
	 * 4)SemaphoreLock
	 */

}
